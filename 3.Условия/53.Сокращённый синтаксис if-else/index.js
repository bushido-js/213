  // if (test == 0) {
  // 	alert('верно');
  // } else {
  // 	alert('неверно!');
  // }

if (test == 0) alert('verno'); else alert('neverno');

// Смысла нет от сокращения, его не рекомендуется использовать, дабы не усложнять себе жизнь